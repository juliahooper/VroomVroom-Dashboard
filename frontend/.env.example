# Copy to .env for local overrides. Used by Vite dev server.

# Backend API URL for proxy (when backend runs on VM or different host).
# Default: http://127.0.0.1:5000
# VITE_API_PROXY=http://192.168.1.100:5000
