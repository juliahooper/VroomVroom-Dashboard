# Python packages this project needs (install with: pip install -r requirements.txt)
# psutil = read CPU, RAM, disk usage from the OS (used by metrics_reader)
# flask = web server for GET /hello and GET /health (used by web_app)
psutil>=5.9.0
flask>=3.0.0
