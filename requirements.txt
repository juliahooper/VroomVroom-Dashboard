# pip install -r requirements.txt
psutil>=5.9.0          # OS metrics (CPU, RAM, disk)
flask>=3.0.0           # Web app
gunicorn>=21.0.0       # Production WSGI
sqlalchemy>=2.0.0      # ORM
psycopg2-binary>=2.9.0 # PostgreSQL (when DATABASE_URL set)
requests>=2.28.0       # HTTP (YouTube API, etc.)
python-dotenv>=1.0.0   # .env loading
firebase-admin>=6.0.0  # Mobile/Firestore (when mobile enabled)
