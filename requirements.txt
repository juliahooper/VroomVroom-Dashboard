# pip install -r requirements.txt
# psutil, flask, gunicorn, sqlalchemy; firebase-admin only needed when mobile.enabled is true
psutil>=5.9.0
flask>=3.0.0
gunicorn>=21.0.0
sqlalchemy>=2.0.0
firebase-admin>=6.0.0
